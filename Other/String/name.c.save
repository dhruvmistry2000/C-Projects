#include<string.h>
#include <stdio.h>


int main()
{char name[20], ch , student[20] ;
int i = 0 , j , n  , len ;


    printf("Enter your name :- ");
    while(ch != '\n')
    {
        ch = getchar();
        name[i] = ch ;
        if (ch != ' ' && ch != '\n')
        {
         i++ ;
        }
        if (ch != ' ' && ch)
        {
         i++ ;
        }

    }
    name[i] = '\0';
    len = strlen(name);
    printf("name : %s \n" , name);
    printf("The lenght of name is =  %d" ,i );

    return 0;
}
